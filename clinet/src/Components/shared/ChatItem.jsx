import React from 'react'

const ChatItem = ({
    avatar=[],
    name,
    _id,
    gropupChats,
    sameSender,
    newMessage,
    index,
    handleDeleteChatOpen    
}) => {
  return (
    <div>
      
    </div>
  )
}

export default ChatItem
